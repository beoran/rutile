( This is the rutile interpreter bootstrap in pre-rutile )

set MODE_PARSE    to_i 1
set MODE_RUN      to_i 2
set MODE_STRING   to_i 3
set MODE_BLOCK    to_i 4
set MODE_COMMENT  to_i 5
set MODE_DONE     to_i 6
set $now          { }
set $mode         get MODE_PARSE
set $blocks       to_i 0
set $escape       false
set $pstack       array 0
set $stack        array 0
set $strend       false
set $comend       false
set $lineno       to_i 0

def parse_input { 
} 

puts "OK"

!dump




